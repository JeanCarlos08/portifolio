# 💈 Premium Barber Manager API

API REST moderna construída com Python e FastAPI para gestão de agendamentos de serviços premium.

## 📌 Sobre o Projeto
Este projeto demonstra o desenvolvimento de um backend robusto, focado em performance e documentação automática. Ele permite gerenciar o fluxo de clientes de uma barbearia, desde a criação do agendamento até o controle de status.

## 🚀 Tecnologias Utilizadas
- **Python 3.12**
- **FastAPI**: Framework web de alta performance.
- **Pydantic**: Para validação rigorosa de esquemas de dados.
- **Uvicorn**: Servidor ASGI para rodar a aplicação.

## ⚙️ Diferenciais Técnicos
- **Swagger UI Automático**: Documentação interativa disponível em `/docs`.
- **Validação de Tipos**: Redução de bugs através do Type Hinting do Python.
- **Arquitetura RESTful**: Seguindo os padrões modernos de comunicação web.

## 🛠️ Como executar
1. Instale as dependências: `pip install -r requirements.txt`.
2. Execute a API: `python main.py`.
3. Acesse `http://localhost:8000/docs` no seu navegador para testar os endpoints interativamente.

## 🧠 Conceitos Aplicados
- Operações CRUD (Create, Read, Update, Delete).
- Tratamento de Exceções HTTP.
- Modelagem de dados com Pydantic.
